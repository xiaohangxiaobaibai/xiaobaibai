<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="../user/pscloud_training.list">
      <field name="name">../user/pscloud_training list</field>
      <field name="model">../user/pscloud_training.../user/pscloud_training</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="../user/pscloud_training.action_window">
      <field name="name">../user/pscloud_training window</field>
      <field name="res_model">../user/pscloud_training.../user/pscloud_training</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="../user/pscloud_training.action_server">
      <field name="name">../user/pscloud_training server</field>
      <field name="model_id" ref="model_../user/pscloud_training_../user/pscloud_training"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="../user/pscloud_training" id="../user/pscloud_training.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="../user/pscloud_training.menu_1" parent="../user/pscloud_training.menu_root"/>
    <menuitem name="Menu 2" id="../user/pscloud_training.menu_2" parent="../user/pscloud_training.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="../user/pscloud_training.menu_1_list" parent="../user/pscloud_training.menu_1"
              action="../user/pscloud_training.action_window"/>
    <menuitem name="Server to list" id="../user/pscloud_training" parent="../user/pscloud_training.menu_2"
              action="../user/pscloud_training.action_server"/>
    -->
  </data>
</odoo>